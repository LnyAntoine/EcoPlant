<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EcoPlant" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.EcoPlant" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.EcoPlant.EditText">
        <item name="android:minWidth">300dp</item>
        <item name="android:minHeight">50dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:background">@drawable/edit_text_style</item>
        <item name="android:textColor">@color/text1_black</item>
        <item name="android:inputType">text</item>
    </style>

    <style name="Theme.EcoPlant.TextView">
        <item name="android:minWidth">300dp</item>
        <item name="android:minHeight">50dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:background">@drawable/edit_text_style</item>
        <item name="android:textColor">@color/text1_black</item>
        <item name="android:inputType">text</item>
    </style>

    <style name="Theme.EcoPlant.MaterialButton" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">16dp</item>
        <item name="android:minWidth">269dp</item>
        <item name="android:textColor">@color/text1_black</item>
        <item name="strokeColor">@color/stroke1_dark_green</item>
        <item name="strokeWidth">1dp</item>
        <item name="backgroundTint">@color/bg1_pale_green</item>
    </style>
</resources>