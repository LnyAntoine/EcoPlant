<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/bg2_pale_green" /> <!-- Couleur de fond -->
    <corners android:radius="64dp" /> <!-- Rayon des coins -->
    <stroke
        android:width="1dp"
        android:color="@color/stroke1_dark_green" />
    <padding android:bottom="10dp"
        android:top="10dp"
        android:left="10dp"
        android:right="10dp"/>
</shape>